connector_type: kafka
bootstrap_server: "broker:29092" 
topic: creditcard-transactions
consumer_group: mage-consumers-dev
auto_offset_reset: latest                   # use "earliest" to replay from start
batch_size: 3
include_metadata: false
api_version: "4.1.1"

# Uncomment the config below to use SSL config
# security_protocol: "SSL"
# ssl_config:
#   cafile: "CARoot.pem"
#   certfile: "certificate.pem"
#   keyfile: "key.pem"
#   password: password
#   check_hostname: true

# Uncomment the config below to use SASL_SSL config
# security_protocol: "SASL_SSL"
# sasl_config:
#   mechanism: "PLAIN"
#   username: username
#   password: password

# Uncomment the config below to use protobuf schema to deserialize message
# serde_config:
#   serialization_method: PROTOBUF
#   schema_classpath: "path.to.schema.SchemaClass"
